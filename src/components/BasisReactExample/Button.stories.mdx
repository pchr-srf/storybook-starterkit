import { Meta, Story, Preview, Source } from '@storybook/addon-docs/blocks';
import { PropsTable } from '@storybook/components';
import LinkTo from '@storybook/addon-links/react';
import ReactDOM from 'react-dom';
import Button from './Button';

<Meta title="Atoms/React-Button" />

# Simple React Button


<Preview>
  <Story name="Default">
    <div>
    <div id="hoidu">hurrdurr</div>
    {() => {
        console.log("wat", <Button />)
        ReactDOM.render(
            <Button />,
            document.getElementById("hoidu")
        );
    }}
    </div>
  </Story>
</Preview>

## Template Props

<PropsTable
  rows={[
    {
      name: 'styleModifier',
      type: { summary: 'string' },
      required: false,
      description: '',
    },
    {
      name: 'description',
      type: { summary: 'string' },
      description: 'Content of the box.'
    },
  ]}
/>
